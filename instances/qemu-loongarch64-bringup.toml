# Kernel-only loongarch64 bring-up (no rootfs image).
# Replaces: make ARCH=loongarch64 BRINGUP=1 run
name = "qemu-loongarch64-bringup"
description = "loongarch64 kernel-only bring-up without filesystem images"
arch = "loongarch64"

[kernel]
bringup = true
