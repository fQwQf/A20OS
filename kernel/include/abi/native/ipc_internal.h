/*
 * A20OS Native ABI — IPC internals re-export.
 * The IPC subsystem is internal (kernel/include/ipc/ipc.h); this header
 * only keeps the old include path working for ABI-layer code.
 */
#ifndef _ABI_NATIVE_IPC_INTERNAL_H
#define _ABI_NATIVE_IPC_INTERNAL_H

#include "ipc/ipc.h"

#endif
